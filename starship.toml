# ~/.config/starship.toml

add_newline = true
command_timeout = 1000
format = """
[╭─](dimmed white) $os$username$hostname$kubernetes$directory$git_branch$git_status$docker_context$nodejs$rust$php$laravel$cmd_duration
[╰─](dimmed white)$character"""


# Drop ugly default prompt characters
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[✗](bold red) "
disabled = false  

# ---

[os]
format = '[$symbol](bold white) '   
disabled = false

[os.symbols]
Windows = ''
Arch = '󰣇'
Ubuntu = ''
Macos = '󰀵'
EndeavourOS = " "
NixOS = " "

# ---

# Shows the username
[username]
style_user = 'white bold'
style_root = 'black bold'
format = '[$user]($style) '
disabled = false
show_always = true

# Shows the hostname
[hostname]
ssh_only = false
format = 'on [$hostname](bold yellow) '
disabled = false

# Shows current directory
[directory]
truncation_length = 1
truncation_symbol = '…/'
home_symbol = '󰋜 ~'
read_only_style = '197'
read_only = '  '
format = 'at [$path]($style)[$read_only]($read_only_style) '

# Shows current git branch
[git_branch]
symbol = ' '
format = 'via [$symbol$branch]($style)'
# truncation_length = 4
truncation_symbol = '…/'
style = 'bold green'

# Shows current git status
[git_status]
format = '[$all_status$ahead_behind]($style) '
style = 'bold green'
conflicted = '🏳'
up_to_date = ''
untracked = ' '
ahead = '⇡${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
behind = '⇣${count}'
stashed = ' '
modified = ' '
staged = '[++\($count\)](green)'
renamed = '襁 '
deleted = ' '

# Shows kubernetes context and namespace
[kubernetes]
format = 'via [󱃾 $context\($namespace\)](bold purple) '
disabled = false

# Docker context
[docker_context]
format = 'via [ $context](bold blue) '
disabled = false

# Node.js version
[nodejs]
format = 'via [ $version](bold green) '
disabled = false

# Rust version
[rust]
format = 'via [🦀 $version](bold red) '
disabled = false

# PHP version
[php]
format = 'via [🐘 $version](bold magenta) '
disabled = false

[vagrant]
disabled = true

[helm]
disabled = true

[python]
disabled = true

[ruby]
disabled = true

[terraform]
disabled = true